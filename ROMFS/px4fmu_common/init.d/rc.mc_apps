#!/bin/sh
#
# Standard apps for multirotors. Attitude/Position estimator, Attitude/Position control.
#
# NOTE: Script variables are declared/initialized/unset in the rcS script.
#

# Start Control Allocator
control_allocator start

# Start Official Multicopter Rate Controller.
# mc_rate_control start

# Start Official Multicopter Attitude Controller.
# mc_att_control start

# Start Pregme Attitude Controller （v2:Renwang-Huang   v1:Hangan-Xu）.
pregme_att_control_v2 start

if param greater -s MC_AT_EN 0
then
	mc_autotune_attitude_control start
fi

# Start Multicopter Position Controller.
mc_hover_thrust_estimator start
flight_mode_manager start

# Start Official Position Controller.
# mc_pos_control start

# Start Pregme Position Controller(v2:Renwang-Huang   v1:Hangan-Xu).
pregme_pos_control_v2 start

# Start Multicopter Land Detector.
land_detector start multicopter

if param compare -s MC_NN_EN 1
then
	mc_nn_control start
fi

if param compare -s MC_RAPTOR_ENABLE 1
then
	mc_raptor start
fi
